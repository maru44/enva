version: '3'

services:
  api:
    container_name: enva-build
     # @TODO comment out
    env_file:
      - .env.prod
    build:
      context: .
      dockerfile: ./infra/docker/go/image/Dockerfile
      target: build
      # dockerfile: ./infra/docker/go/build/Dockerfile
    tty: true
    volumes:
      - .:/enva
